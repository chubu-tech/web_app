# Real salon photography goes here.
#
# Drop JPG/WebP files in this folder and point lib/content.ts at
# `/photos/<name>.jpg` instead of the Unsplash placeholders. Once no remote
# URLs remain, delete the `images.remotePatterns` entry in next.config.ts.
